spring:
  profiles:
    active: test
  jpa:
    database: h2
    hibernate:
      ddl-auto: create
  datasource:
    url: jdbc:h2:mem:db;DB_CLOSE_DELAY=-1
    username: sa
    password: sa
  flyway:
    enabled: false

crawler:
  storage:
    folder: /tmp/crawler4j/

logging:
  file:
    path: "src/test/resources/logs"

90minutes:
  players:
    number: 42587

article:
  daysback: 3
